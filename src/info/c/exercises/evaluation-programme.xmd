::exercise evaluation-programme
::title Évaluer ce que fait un programme C
::tags code C
::courses IF2 LO21
::source PL$tek
::question difficulty=2
	::text
		Déterminer ce qu'affiche le programme suivant :

		```c
		int main() {
			int x = 1, y = 2;
			y *= 16;
			x += 2;
			x = y / x + x;
			y = x * y;
			printf("%d, %d\n", x, y);
			return 0;
		}```
	::hint 2
		Attention, ça n'utilise que des entiers
	::hint 3
		La division est entière
	::answer
		Le programme affiche `13, 416` :

		```c
		int main() {
			int x = 1, y = 2;  // x = 1, y = 2
			y *= 16;           // x = 1, y = 32
			x += 2;            // x = 3, y = 32
			x = y / x + x;     // x = 32/3 + 3 = 10 + 3 = 13, y = 32 (division entière)
			y = x * (y - 2);   // x = 13, y = 390
			printf("%d, %d\n", x, y);
			return 0;
		}```
